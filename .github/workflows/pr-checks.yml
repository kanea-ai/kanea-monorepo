name: PR Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  code-quality-and-security:
    name: Pre-commit & Detect Secrets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required to scan full git history for secrets

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.11.0'

      - name: Cache pre-commit environments
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ runner.os }}-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            pre-commit-${{ runner.os }}-

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Run pre-commit hooks
        # --show-diff-on-failure prints the formatting changes Black/Prettier wanted to make
        run: pre-commit run --show-diff-on-failure --color=always --all-files

  # Uncomment this job once the Nx workspace is fully populated
  # monorepo-checks:
  #   name: Nx Affected (Lint & Test)
  #   runs-on: ubuntu-latest
  #   needs: code-quality-and-security
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20.11.0'
  #
  #     # Assuming pnpm based on your exclude list. Change to npm/yarn if needed.
  #     - name: Install pnpm
  #       uses: pnpm/action-setup@v3
  #       with:
  #         version: 9
  #
  #     - name: Install Dependencies
  #       run: pnpm install
  #
  #     - name: Run Nx Affected
  #       run: |
  #         pnpm exec nx affected -t lint
  #         pnpm exec nx affected -t test
